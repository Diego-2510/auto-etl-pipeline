database:
  path: "data/market_data.db"

extract:
  source: "yfinance"

  symbols:
    - "AAPL"
    - "MSFT"
    - "BTC-USD"

  period: "1y"
  interval: "1d"

cache:
  enabled: true
  directory: "data/cache"
  max_age_hours: 24

logging:
  level: "INFO"
  directory: "logs"